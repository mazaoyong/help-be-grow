import IronBase from '@youzan/iron-base';

// eslint-disable-next-line
new IronBase({ ROOT_PATH: process.env.ROOT_PATH });

// app.on('start', (app: IronBase) => {
//   app.logger.info('wsc-h5-vis start.');
// });
