export default {
  loading(state, loading) {
    state.loading = loading;
  },

  list(state, list) {
    state.list = list;
  },

  sortType(state, sortType) {
    state.sortType = sortType;
  },
};
