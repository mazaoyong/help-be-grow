import { ActionTree } from 'vuex';
import { ExamState } from './state';

interface AnswerActions extends ActionTree<ExamState, ExamState> {
}

const actions: AnswerActions = {
};

export default actions;
